# Code generated by "scratch generate config". DO NOT EDIT.

# В этом файле содержатся системные переменные, предоставляемые платформой.
# Он необходим только для доставки переменных в etcd и перезатирается каждый
# раз, когда вы вызываете команду "make generate".
# Файл не предназначен для редактирования. Все манипуляции с переменными,
# имеющимися здесь необходимо проводить с помощью ITC.
#
# Если вам необходимо добавить собственную переменную - воспользуйтесь файлами
# values.yaml, values_development.yaml, values_staging.yaml и values_production.yaml.
#
# Дополнительная документация о добавлении переменных:
# https://gitlab.ozon.ru/platform/scratch#добавление-переменных

realtimeConfig:
    audit_user_to_service_automatic_logging_enabled:
        usage: Включает автоматический аудит http запросов с парсингом пользовательского токена. Логи пишутся автоматически через http middleware.Данные логи не отображаются в graylog, так как вычитываются отдельно. Данный конфиг регулирует только автоматические логи.(перезагрузка не требуется)
        group: scratch
        value: false
        type: bool
        displayName: Автоматические аудит логи http запросов с парсингом пользовательского токена
    circuit_breakers:
        usage: Правила CircuitBreaker для клиентов grpc
        group: scratch
        value: '{"__default__":{"disabled":false,"opener":{"err_percent":20}}}'
        type: string
        displayName: Правила CircuitBreaker
    circuit_breakers_verbose_err:
        usage: Включает детализацию ошибок при открытии circuit breaker. Рестарт сервиса не требуется.
        group: scratch
        value: false
        type: bool
        displayName: Детализация ошибок circuit breaker.
    clientside_balancing_diagnostics:
        usage: Настройки для диагностики клиентской балансировки
        group: scratch
        value: ""
        type: string
        displayName: Диагностика клиентской балансировки
    external_services_overrides:
        usage: 'Настройки походов во внешние сервисы: s2s, discovery и т.д. (нужно рестартить)'
        group: scratch
        value: ""
        type: string
        displayName: Настройки внешних сервисов
    gc_memory_ballast_size:
        usage: Размер балластной памяти для уменьшения частоты вызовов GC (требуется перезагрузка; KB/MB/GB)
        group: scratch
        value: 0MB
        type: string
        displayName: Размер балластной памяти GC
    gogc:
        usage: Задает целевой процент GC
        group: scratch
        value: 100
        type: int
        displayName: GOGC
    gomaxprocs:
        usage: Устанавливает значение GOMAXPROCS
        group: scratch
        value: auto
        type: string
        displayName: GOMAXPROCS
    gomemlimit:
        usage: 'Размер soft memory limit (KB/MB/GB/KiB/MiB/GiB), можно задать так же число, оно будет расценено как количество байт. Работает только на Go 1.19+. Значение auto установит gomemlimit автоматически в 90% от лимита по памяти в Kubernetes, так же можно указать проценты (например: "80%") от лимита. Значение default или пустая строка установит значение по умолчанию math.MaxInt64.'
        group: scratch
        value: default
        type: string
        displayName: GOMEMLIMIT
    grpc_call_options:
        usage: Настройки опций запроса gRPC
        group: scratch
        value: '{"__default__": {"max_call_recv_msg_size_bytes": 33554432, "max_call_recv_msg_size_threshold_bytes": 4194304, "snappy": false}}'
        type: string
        displayName: Опции запроса gRPC
    grpc_server_initial_conn_window_size:
        usage: Устанавливает "размер окна" для соединения grpc. Нижняя граница размера окна составляет 64 КБ, и любое значение, меньшее этого, будет игнорироваться
        group: scratch
        value: 65535
        type: int
        displayName: Размер окна для соединения grpc
    grpc_server_initial_window_size:
        usage: 'Устанавливает "размер окна" для стрима grpc. Нижняя граница размера окна составляет 64 КБ, и любое значение, меньшее этого, будет игнорироваться(требуется перезагрузка; размер в байтах; дефолтное значение: 64KB)'
        group: scratch
        value: 65535
        type: int
        displayName: Размер окна для стрима grpc
    grpc_server_max_concurrent_streams:
        usage: 'Будет применяться ограничение на количество одновременных потоков для каждого ServerTransport (требуется перезагрузка; дефолтное значение: 0)'
        group: scratch
        value: 0
        type: uint
        displayName: Максимальное количество параллельных grpc потоков
    grpc_server_max_recv_msg_size:
        usage: 'Задает максимальный размер сообщения в байтах, которое может получить сервер (требуется перезагрузка; размер в байтах; дефолтное значение: 4MB)'
        group: scratch
        value: 4194304
        type: int
        displayName: Максимальный размер сообщения на получение grpc
    grpc_server_max_send_msg_size:
        usage: 'Задает максимальный размер сообщения в байтах, которое может послать сервер (требуется перезагрузка; размер в байтах; дефолтное значение: math.MaxInt32)'
        group: scratch
        value: 2147483647
        type: int
        displayName: Максимальный размер сообщения на отправку grpc
    grpc_server_read_buffer_size:
        usage: Определяет какой объем данных может быть прочитан за один системный вызов (требуется перезагрузка; размер в байтах; дефолтное значение 32KB)
        group: scratch
        value: 32768
        type: int
        displayName: Размер буфера grpc для чтения за один системный вызов
    grpc_server_stream_worker_count:
        usage: Количество горутин на сервере обрабатывающих входящие потоки grpc (требуется перезагрузка)
        group: scratch
        value: 0
        type: uint
        displayName: Количество серверных горутин для обработки потоков grpc
    grpc_server_write_buffer_size:
        usage: Определяет, какой объем данных может быть собран перед выполнением записи на транспортном уровне (требуется перезагрузка; размер в байтах; дефолтное значение 32KB)
        group: scratch
        value: 32768
        type: int
        displayName: Размер буфера grpc для записи перед отправкой
    gzip_limit_request_size_enabled:
        usage: Включает ограничения для размера запроса gzip
        group: scratch
        value: 0
        type: int64
        displayName: Ограничение запроса gzip
    hedged_requests:
        usage: Правила повторных запросов
        group: scratch
        value: ""
        type: string
        displayName: Политика HedgedRequests
    log_ip_of_unknown_clients_enabled:
        usage: Включение логирования ip адресов клиентское имя которых unknown
        group: scratch
        value: "false"
        type: bool
        displayName: Логирование неизвестных клиентов
    metrics_db_response_time_enabled:
        usage: Включает метрику времени ответа базы (pgsql/mssql)
        group: scratch
        value: true
        type: bool
        displayName: Метрика времени ответа базы
    metrics_db_response_time_query_label_enabled:
        usage: Включает лэйбл query в метрике времени ответа базы (pgsql/mssql)
        group: scratch
        value: true
        type: bool
        displayName: Лейбл query в метрике времени ответа базы
    metrics_grpc_call_options_enabled:
        usage: Включает метрики для gRPC запроса
        group: scratch
        value: true
        type: bool
        displayName: Метрики для gRPC запроса
    metrics_grpc_header_size_enabled:
        usage: Включает метрику размера заголовков gRPC
        group: scratch
        value: false
        type: bool
        displayName: Метрика размера заголовков gRPC
    metrics_grpc_payload_size_enabled:
        usage: Включает метрику размера тела сообщения gRPC
        group: scratch
        value: false
        type: bool
        displayName: Метрика размера тела сообщения gRPC
    metrics_request_client_name_enabled:
        usage: Включает лэйбл client_name в метриках запроса HTTP/gRPC
        group: scratch
        value: false
        type: bool
        displayName: Лэйбл client_name в метриках запроса HTTP/gRPC
    overload_ammo_collector_agent_enabled:
        usage: Включает агента overload по сборке патронов
        group: scratch
        value: false
        type: bool
        displayName: Включение сборки патронов
    rate_limits:
        usage: Правила ограничения скорости входящего трафика
        group: scratch
        value: ""
        type: string
        displayName: Правила RateLimiter
    request_body_size:
        usage: Включение детализации по размерам запросов (по умолчанию выключена)
        group: scratch
        value: false
        type: bool
        displayName: Детализация размеров запросов
    requests_status_code_aggregation:
        usage: Включение агрегации статус кодов запросов на стороне сервиса (влияет на формат статус кодов запросов)
        group: scratch
        value: true
        type: bool
        displayName: Агрегация статус кодов запросов
    response_body_size:
        usage: Включение детализации по размерам ответов (по умолчанию выключена)
        group: scratch
        value: false
        type: bool
        displayName: Детализация размеров ответов
    s2s_auth_requests_sign_grpc:
        usage: Получать токены с сервера аутентификации и включать во все исходящие запросы
        group: scratch
        value: try-sign
        type: string
        view:
            enum:
                - disabled
                - try-sign
                - sign
        displayName: s2s подпись по grpc
    s2s_auth_requests_sign_http:
        usage: Получать токены с сервера аутентификации и включать во все исходящие запросы
        group: scratch
        value: try-sign
        type: string
        view:
            enum:
                - disabled
                - try-sign
                - sign
        displayName: s2s подпись по http
    s2s_auth_requests_verify_cache_size:
        usage: Размер кеша валидации токенов s2s из входящих запросов (требуется перезагрузка; кол-во кешируемых токенов s2s; по умолчанию выключен)
        group: scratch
        value: 0
        type: int
        displayName: Размер кеша верификации s2s авторизации
    s2s_auth_requests_verify_grpc:
        usage: На входящие запросы без действительного токена выдается ответ с ошибкой
        group: scratch
        value: observe-only
        type: string
        view:
            enum:
                - disabled
                - observe-only
                - reject
        displayName: s2s авторизация grpc
    s2s_auth_requests_verify_http:
        usage: На входящие запросы без действительного токена выдается ответ с ошибкой
        group: scratch
        value: observe-only
        type: string
        view:
            enum:
                - disabled
                - observe-only
                - reject
        displayName: s2s авторизация http
